const DependencyInversion = () => {
  return (
    <>
      High-level modules should not depend on low-level modules. Instead, both
      should depend on abstractions.
    </>
  );
};

export default DependencyInversion;
